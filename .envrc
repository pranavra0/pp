# Standardized pp development environment.
#
# Build the saved Lisp image explicitly before using the launcher:
#
#   scripts/build-lisp.sh --output lisp/pp
#   pp file.pp
#
# bin/pp is the accepted executable launcher and never compiles at invocation.
PATH_add bin
