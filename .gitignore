# Build artifacts
*.o

# Saved Lisp image and launcher payload.
/lisp/pp
/lisp/pp.sbcl-image
/lisp/pp.sbcl-image.build-id
/pp
/pp-native

# Tool caches
_build/

# Cache
.pp/

# Fuzzer
fuzz
fuzz-failures/
fuzz-failures-full/
scripts/.cache/

# manual build artifacts (regenerated by scripts/build-manual.sh)
# docs/manual/site/ (index.html, pp-manual.pdf) is committed — the rendered
# manual, kept in sync by hand after chapter edits; only the intermediate
# capture cache stays ignored.
docs/manual/captured/

# Compiled fasls must never sit beside source.
*.fasl
