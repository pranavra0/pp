# pp build artifacts
*.cmo
*.cmi
*.cmx
*.o

# pp binary — root convenience symlink (bin/pp is tracked instead)
/pp
/pp-native

# Cache
.pp/

# OCaml
_build/

# Fuzzer
fuzz
fuzz-failures/
fuzz-failures-full/
scripts/.cache/

# manual build artifacts (regenerated by scripts/build-manual.sh)
docs/manual/site/
docs/manual/captured/
