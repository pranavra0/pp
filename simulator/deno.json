{
  "tasks": {
    "generate": "deno run --allow-read=../src/runtime/event.ml --allow-write=src/events.generated.ts scripts/generate_events.ts",
    "test": "deno task runtime && deno task generate && deno check controller.ts lab.ts generate.ts && deno test --allow-read=fixtures,src src && node scripts/runtime_smoke.js",
    "runtime": "cd .. && dune build src/browser/pp_browser.bc.js",
    "build": "deno task runtime && deno task generate && deno check src/app.ts && deno run --allow-read=src,fixtures,../_build/default/src/browser/pp_browser.bc.js --allow-write=site scripts/build.ts && deno bundle --platform=browser --minify --sourcemap -o site/app.js src/app.ts",
    "check": "deno task test && deno task build && deno run --allow-read=site scripts/deployment_smoke.ts",
    "lab": "deno run --allow-read --allow-write --allow-run --allow-env lab.ts",
    "controller": "deno run --allow-net=127.0.0.1 --allow-read --allow-run --allow-write --allow-env controller.ts",
    "workload": "deno run generate.ts"
  },
  "imports": {
    "immutable": "npm:immutable@5.1.9"
  },
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable", "deno.ns"],
    "strict": true,
    "noUncheckedIndexedAccess": true
  }
}
