; The differential fuzzer (docs/TESTING.md). Stdlib + unix only.
; Run:  dune exec fuzz -- --grammar full --count 1500
(executable
 (name fuzz)
 (modules fuzz)
 (libraries unix)
 (flags -no-strict-sequence))
