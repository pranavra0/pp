# Saved Lisp image and launcher payload.
/lisp/pp
/lisp/pp.sbcl-image
/lisp/pp.sbcl-image.build-id

# pp runtime store/cache
.pp/

# Manual build outputs are deployment artefacts, regenerated in CI.
docs/manual/site/
docs/manual/captured/

# Compiled fasls must never sit beside source.
*.fasl
