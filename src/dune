(executable
 (name main)
 (modules types hasher runtime store reader capabilities process reconciler
   supervisor fenced island primitives evaluator compiler stabilize vm repl main)
 (libraries cryptokit)
 (flags -no-strict-sequence))
