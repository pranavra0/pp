# This file is generated by dune, edit dune-project instead
opam-version: "2.0"
version: "0.2.0-dev"
synopsis: "A content-addressed, capability-scoped Lisp"
description:
  "pp is a content-addressed, capability-scoped Lisp with two back ends (a tree-walking interpreter and a bytecode VM) that must produce identical output. See AGENTS.md and docs/ for details."
depends: [
  "dune" {>= "3.0"}
  "ocaml" {>= "5.1"}
  "dune-build-info"
  "cryptokit"
  "odoc" {with-doc}
]
build: [
  ["dune" "subst"] {dev}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test}
    "@doc" {with-doc}
  ]
]
