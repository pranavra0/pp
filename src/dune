(executable
 (name main)
 (modules types hasher codec paths cell runtime store journal reader capabilities process
   reconciler supervisor fenced island primitives evaluator compiler stabilize vm
   repl main)
 (libraries cryptokit)
 (flags -no-strict-sequence))
